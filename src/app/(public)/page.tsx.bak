'use client'

import Link from 'next/link'

const plans = [
  {
    name: 'Starter',
    price: 'GH₵50',
    period: '/month',
    description: 'Perfect for small businesses',
    features: [
      '1 Location',
      '100 Products',
      '2 Users',
      'POS & Sales',
      'Credit Sales',
      'Expiration Alerts',
      'Basic Reports',
    ],
    notIncluded: ['Barcode Scanning', 'Returns', 'Invoices'],
    highlighted: false,
  },
  {
    name: 'Professional',
    price: 'GH₵120',
    period: '/month',
    description: 'For growing businesses',
    features: [
      '3 Locations',
      '500 Products',
      '5 Users',
      'Everything in Starter',
      'Barcode Scanning',
      'Returns & Expenses',
      'Invoice Generation',
      'Advanced Reports',
    ],
    notIncluded: [],
    highlighted: true,
  },
  {
    name: 'Business',
    price: 'GH₵250',
    period: '/month',
    description: 'For established businesses',
    features: [
      '10 Locations',
      '2,000 Products',
      '15 Users',
      'Everything in Professional',
      'Multi-currency',
      'API Access',
    ],
    notIncluded: [],
    highlighted: false,
  },
  {
    name: 'Enterprise',
    price: 'GH₵500',
    period: '/month',
    description: 'For large operations',
    features: [
      'Unlimited Locations',
      'Unlimited Products',
      'Unlimited Users',
      'Everything in Business',
      'Priority Support',
      'Custom Branding',
    ],
    notIncluded: [],
    highlighted: false,
  },
]

export default function LandingPage() {
  return (
    <div className="landing">
      {/* Hero Section */}
      <header className="hero">
        <nav className="nav">
          <div className="logo">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" />
              <polyline points="3.27 6.96 12 12.01 20.73 6.96" />
              <line x1="12" y1="22.08" x2="12" y2="12" />
            </svg>
            <span>Inventory Pro</span>
          </div>
          <div className="nav-links">
            <a href="#pricing">Pricing</a>
            <a href="#features">Features</a>
            <Link href="/login" className="btn btn-ghost">Login</Link>
            <Link href="/signup" className="btn btn-primary">Start Free Trial</Link>
          </div>
        </nav>

        <div className="hero-content">
          <h1>Smart Inventory Management for Your Business</h1>
          <p>Track products, manage sales, handle credit customers, and grow your business with ease.</p>
          <div className="hero-cta">
            <Link href="/signup" className="btn btn-primary btn-lg">
              Start 14-Day Free Trial
            </Link>
            <span className="hero-note">No credit card required</span>
          </div>
        </div>
      </header>

      {/* Features Section */}
      <section id="features" className="features-section">
        <h2>Everything You Need</h2>
        <div className="features-grid">
          <div className="feature-card">
            <div className="feature-icon blue">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" />
              </svg>
            </div>
            <h3>Inventory Tracking</h3>
            <p>Track stock levels, get low stock alerts, and manage products across locations.</p>
          </div>

          <div className="feature-card">
            <div className="feature-icon green">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
                <line x1="1" y1="10" x2="23" y2="10" />
              </svg>
            </div>
            <h3>Point of Sale</h3>
            <p>Fast, intuitive POS for quick sales with cash, card, or credit options.</p>
          </div>

          <div className="feature-card">
            <div className="feature-icon yellow">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                <circle cx="9" cy="7" r="4" />
                <line x1="23" y1="11" x2="17" y2="11" />
              </svg>
            </div>
            <h3>Credit Sales</h3>
            <p>Sell on credit, track customer balances, and record payments easily.</p>
          </div>

          <div className="feature-card">
            <div className="feature-icon red">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <circle cx="12" cy="12" r="10" />
                <polyline points="12 6 12 12 16 14" />
              </svg>
            </div>
            <h3>Expiration Alerts</h3>
            <p>Never lose money on expired products. Get alerts before items expire.</p>
          </div>

          <div className="feature-card">
            <div className="feature-icon blue">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <line x1="18" y1="20" x2="18" y2="10" />
                <line x1="12" y1="20" x2="12" y2="4" />
                <line x1="6" y1="20" x2="6" y2="14" />
              </svg>
            </div>
            <h3>Reports</h3>
            <p>Sales, stock, credit, and expense reports to understand your business.</p>
          </div>

          <div className="feature-card">
            <div className="feature-icon green">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                <polyline points="14 2 14 8 20 8" />
                <line x1="16" y1="13" x2="8" y2="13" />
                <line x1="16" y1="17" x2="8" y2="17" />
              </svg>
            </div>
            <h3>Invoices</h3>
            <p>Generate professional invoices and send them to customers.</p>
          </div>
        </div>
      </section>

      {/* Pricing Section */}
      <section id="pricing" className="pricing-section">
        <h2>Simple, Transparent Pricing</h2>
        <p className="pricing-subtitle">Start free for 14 days. No credit card required.</p>

        <div className="pricing-grid">
          {plans.map((plan) => (
            <div key={plan.name} className={`pricing-card ${plan.highlighted ? 'highlighted' : ''}`}>
              {plan.highlighted && <div className="popular-badge">Most Popular</div>}
              <h3>{plan.name}</h3>
              <div className="price">
                <span className="amount">{plan.price}</span>
                <span className="period">{plan.period}</span>
              </div>
              <p className="plan-description">{plan.description}</p>
              <Link href="/signup" className={`btn ${plan.highlighted ? 'btn-primary' : 'btn-secondary'} w-full`}>
                Start Free Trial
              </Link>
              <ul className="feature-list">
                {plan.features.map((feature) => (
                  <li key={feature}>
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                      <polyline points="20 6 9 17 4 12" />
                    </svg>
                    {feature}
                  </li>
                ))}
                {plan.notIncluded.map((feature) => (
                  <li key={feature} className="not-included">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                      <line x1="18" y1="6" x2="6" y2="18" />
                      <line x1="6" y1="6" x2="18" y2="18" />
                    </svg>
                    {feature}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </section>

      {/* CTA Section */}
      <section className="cta-section">
        <h2>Ready to Get Started?</h2>
        <p>Join thousands of businesses managing their inventory smarter.</p>
        <Link href="/signup" className="btn btn-primary btn-lg">
          Start Your Free Trial
        </Link>
      </section>

      {/* Footer */}
      <footer className="footer">
        <p>&copy; 2024 Inventory Pro. All rights reserved.</p>
      </footer>

      <style jsx>{`
        .landing {
          min-height: 100vh;
        }

        .hero {
          background: linear-gradient(135deg, #0284c7 0%, #0369a1 100%);
          color: white;
          padding: var(--spacing-4) var(--spacing-6);
          min-height: 500px;
        }

        .nav {
          display: flex;
          justify-content: space-between;
          align-items: center;
          max-width: 1200px;
          margin: 0 auto;
          padding: var(--spacing-2) 0;
        }

        .logo {
          display: flex;
          align-items: center;
          gap: var(--spacing-2);
          font-size: var(--font-size-xl);
          font-weight: var(--font-weight-bold);
        }

        .nav-links {
          display: flex;
          align-items: center;
          gap: var(--spacing-4);
        }

        .nav-links a {
          color: white;
        }

        .hero-content {
          max-width: 800px;
          margin: var(--spacing-16) auto;
          text-align: center;
        }

        .hero-content h1 {
          font-size: 3rem;
          font-weight: var(--font-weight-bold);
          margin-bottom: var(--spacing-4);
          line-height: 1.2;
        }

        .hero-content p {
          font-size: var(--font-size-xl);
          opacity: 0.9;
          margin-bottom: var(--spacing-6);
        }

        .hero-cta {
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: var(--spacing-2);
        }

        .hero-note {
          font-size: var(--font-size-sm);
          opacity: 0.8;
        }

        .features-section {
          padding: var(--spacing-16) var(--spacing-6);
          max-width: 1200px;
          margin: 0 auto;
        }

        .features-section h2 {
          text-align: center;
          font-size: var(--font-size-3xl);
          margin-bottom: var(--spacing-10);
        }

        .features-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
          gap: var(--spacing-6);
        }

        .feature-card {
          padding: var(--spacing-6);
          border: 1px solid var(--color-border);
          border-radius: var(--radius-xl);
        }

        .feature-icon {
          width: 48px;
          height: 48px;
          border-radius: var(--radius-lg);
          display: flex;
          align-items: center;
          justify-content: center;
          margin-bottom: var(--spacing-4);
        }

        .feature-icon.blue { background: var(--color-primary-100); color: var(--color-primary-600); }
        .feature-icon.green { background: var(--color-success-100); color: var(--color-success-600); }
        .feature-icon.yellow { background: var(--color-warning-100); color: var(--color-warning-600); }
        .feature-icon.red { background: var(--color-danger-100); color: var(--color-danger-600); }

        .feature-card h3 {
          font-size: var(--font-size-lg);
          margin-bottom: var(--spacing-2);
        }

        .feature-card p {
          color: var(--color-text-secondary);
        }

        .pricing-section {
          background: var(--color-bg-secondary);
          padding: var(--spacing-16) var(--spacing-6);
        }

        .pricing-section h2 {
          text-align: center;
          font-size: var(--font-size-3xl);
          margin-bottom: var(--spacing-2);
        }

        .pricing-subtitle {
          text-align: center;
          color: var(--color-text-secondary);
          margin-bottom: var(--spacing-10);
        }

        .pricing-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
          gap: var(--spacing-4);
          max-width: 1200px;
          margin: 0 auto;
        }

        .pricing-card {
          background: white;
          border: 1px solid var(--color-border);
          border-radius: var(--radius-xl);
          padding: var(--spacing-6);
          position: relative;
        }

        .pricing-card.highlighted {
          border-color: var(--color-primary-500);
          box-shadow: 0 0 0 2px var(--color-primary-100);
        }

        .popular-badge {
          position: absolute;
          top: -12px;
          left: 50%;
          transform: translateX(-50%);
          background: var(--color-primary-600);
          color: white;
          padding: var(--spacing-1) var(--spacing-3);
          border-radius: var(--radius-full);
          font-size: var(--font-size-xs);
          font-weight: var(--font-weight-semibold);
        }

        .pricing-card h3 {
          font-size: var(--font-size-xl);
          margin-bottom: var(--spacing-2);
        }

        .price {
          margin-bottom: var(--spacing-2);
        }

        .price .amount {
          font-size: var(--font-size-3xl);
          font-weight: var(--font-weight-bold);
        }

        .price .period {
          color: var(--color-text-secondary);
        }

        .plan-description {
          color: var(--color-text-secondary);
          font-size: var(--font-size-sm);
          margin-bottom: var(--spacing-4);
        }

        .feature-list {
          list-style: none;
          margin-top: var(--spacing-4);
        }

        .feature-list li {
          display: flex;
          align-items: center;
          gap: var(--spacing-2);
          padding: var(--spacing-2) 0;
          font-size: var(--font-size-sm);
          color: var(--color-text-primary);
        }

        .feature-list li svg {
          color: var(--color-success-500);
          flex-shrink: 0;
        }

        .feature-list li.not-included {
          color: var(--color-text-muted);
        }

        .feature-list li.not-included svg {
          color: var(--color-text-muted);
        }

        .cta-section {
          text-align: center;
          padding: var(--spacing-16) var(--spacing-6);
        }

        .cta-section h2 {
          font-size: var(--font-size-3xl);
          margin-bottom: var(--spacing-2);
        }

        .cta-section p {
          color: var(--color-text-secondary);
          margin-bottom: var(--spacing-6);
        }

        .footer {
          text-align: center;
          padding: var(--spacing-8) var(--spacing-6);
          border-top: 1px solid var(--color-border);
          color: var(--color-text-muted);
          font-size: var(--font-size-sm);
        }

        .btn-lg {
          padding: var(--spacing-4) var(--spacing-8);
          font-size: var(--font-size-lg);
        }

        .w-full {
          width: 100%;
        }

        @media (max-width: 768px) {
          .hero-content h1 {
            font-size: 2rem;
          }

          .nav-links a:not(.btn) {
            display: none;
          }
        }
      `}</style>
    </div>
  )
}
